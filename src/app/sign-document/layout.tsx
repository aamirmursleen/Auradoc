import { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Sign Documents Online Free - E-Signature',
  description: 'Sign PDF documents electronically with legally-binding digital signatures. Add multiple signers, fields, and send for signing.',
}

export default function Layout({ children }: { children: React.ReactNode }) { return <>{children}</> }
